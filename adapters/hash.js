var enabled = require('enabled');

/**
 * Check the window.location.hash to see if has any debug params that can
 * are allowed to flag.
 *
 * @param {String} namespace The namespace we're given
 */
module.exports = function hashAdapter(namespace) {

};
